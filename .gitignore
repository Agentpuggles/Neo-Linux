# Byte-compiled / caches
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Local dev environments
.venv/
venv/

# Scratch output from testing the launcher by hand
NEO_HOME/
*.log
neo-install-*/

# The official NeoFN Windows launcher, kept locally as protocol reference only.
# It is third-party copyrighted software (NeoLauncher.exe, Epic's
# BuildPatchServices.dll, Microsoft redistributables, NeoFN media) that this
# project has no right to redistribute, so it must never be committed.
# See CONTRIBUTING.md → The Windows reference copy.
original-windows-launcher/

# Editor and OS droppings
.idea/
.vscode/
*.swp
.DS_Store

# Build output belongs in release artifacts, never in Git. Game data is local too.
dist/
build/
*.egg-info/
CloudDir/
*.chunk

*.AppImage
*.AppImage.sha256
*.AppImage.build-info.json
